plugins {
    id("com.android.application") version "8.2.2"
    id("org.jetbrains.kotlin.android") version "1.9.22"
    id("org.jetbrains.kotlin.kapt") version "1.9.22"
    id("com.chaquo.python") version "16.0.0"

}

apply plugin: 'com.chaquo.python'

def GEMINI_API_KEY = project.hasProperty("GEMINI_API_KEY") ? project.GEMINI_API_KEY : ""

android {
    namespace = "com.upidea.astrolumina"
    compileSdk = 34

    buildFeatures {
        viewBinding true
        buildConfig true
    }

    defaultConfig {
        applicationId = "com.upidea.astrolumina"
        minSdk = 24
        targetSdk = 34
        versionCode = 1
        versionName = "1.0"
        // Gemini API Key'i BuildConfig'e geçiriyoruz
        buildConfigField "String", "GEMINI_API_KEY", "\"${GEMINI_API_KEY}\""


        python {
            version "3.8"
            pip {
                install "sgp4==2.21" // Add this line
                install "skyfield"
                install "pytz"
            }
        }
        ndk {
            abiFilters "arm64-v8a", "x86_64"
        }
    }

    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile("proguard-android-optimize.txt"), "proguard-rules.pro"

        }
    }

    compileOptions {
        sourceCompatibility = JavaVersion.VERSION_17
        targetCompatibility = JavaVersion.VERSION_17
    }

    kotlinOptions {
        jvmTarget = "17"
    }
}

dependencies {
    implementation("androidx.core:core-ktx:1.12.0")
    implementation("androidx.appcompat:appcompat:1.6.1")
    implementation("com.google.android.material:material:1.11.0")
    implementation("androidx.lifecycle:lifecycle-runtime-ktx:2.6.2")
    implementation 'androidx.constraintlayout:constraintlayout:2.1.4'
    implementation 'com.google.android.material:material:1.11.0'




    // Room database
    implementation("androidx.room:room-runtime:2.6.1")
    kapt("androidx.room:room-compiler:2.6.1")
    implementation("androidx.room:room-ktx:2.6.1")

    implementation("com.squareup.okhttp3:okhttp:4.12.0")
}
